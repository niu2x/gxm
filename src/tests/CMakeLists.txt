function(build_application name)
	add_executable(${name} "")
	file(GLOB_RECURSE sources PATTERN "${name}/*.cpp")
	target_sources(${name} PRIVATE ${sources})
	target_link_libraries(${name} PRIVATE gxm)
endfunction()

# build_application(blend)
build_application(log)
build_application(entry)
